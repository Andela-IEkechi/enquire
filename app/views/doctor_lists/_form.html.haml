= form_for @doctor_list do |f|
  - if @doctor_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@doctor_list.errors.count, "error")} prohibited this doctor_list from being saved:"
      %ul
        - @doctor_list.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :user
    = f.text_field :user
  .field
    = f.label :hospital
    = f.text_field :hospital
  .actions
    = f.submit 'Save'
