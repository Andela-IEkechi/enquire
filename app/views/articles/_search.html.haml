= form_tag [:articles], {:method => :get, :class => 'navbar-search pull-right', style:'margin-right:15px;'} do
  = text_field_tag 'q', @q, { placeholder: "Search Articles", autocomplete: articles_autocomplete_article_title_path }